# 反转一个 链表

```javascript
const node = {  
val:  
next:  
}  
  
var reverseList = function(head) {  
if (!head) { return head; }  
  
let pre = null;  
let cur = head;  
  
while (cur) {  
const { next } = cur;  
cur.next = pre;  
pre = cur;  
cur = next;  
}  
  
return pre;  
};

```
