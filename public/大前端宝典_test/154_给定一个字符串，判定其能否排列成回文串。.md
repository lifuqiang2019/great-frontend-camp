# 给定一个字符串，判定其能否排列成回文串。

```javascript
var canPermutePalindrome = function(s) {  
const set = new Set();  
s.split('').forEach(key => {  
if (set.has(key)) {  
set.delete(key);  
} else {  
set.add(key);  
}  
});  
return set.size <= 1;  
};

```
